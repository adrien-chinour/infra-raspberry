twig:
    default_path: '%kernel.project_dir%/templates'
    globals:
        transmission_url: '%app.transmission_url%'
        plex_url: '%app.plex_url%'
        sonarr_url: '%app.sonarr_url%'
        radarr_url: '%app.radarr_url%'
